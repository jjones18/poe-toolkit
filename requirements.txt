# Full runtime compatibility set. Prefer the locked pyproject install:
#   uv sync --locked --extra full

# Core UI/API
PyQt6>=6.6,<7
requests>=2.31,<3

# Overlay click detection
pynput>=1.7.6,<2

# Platform window detection
pywin32>=311; sys_platform == "win32"
python-xlib>=0.33,<1; sys_platform == "linux"

# OCR and image capture (Tesseract executable is also required)
mss>=10,<11
numpy>=2,<2.3
opencv-python>=4.10,<5
pytesseract>=0.3.13,<0.4
